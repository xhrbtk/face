import { INativeResponse, ModuleBase } from '../native';
declare type acc_type = 'alipay' | 'wx' | 'wechat';
interface IWithdrawParams {
    money: number;
    acc_type: acc_type;
}
interface IBindParams {
    acc_type: acc_type;
}
interface callPayParam {
    money: number;
    orderid: string;
    transaction_type: 'greenpacket' | 'vip';
}
interface passwordParam {
    password: number | string;
    verify_code: number | string;
    apply_token: number | string;
}
declare class WalletModule extends ModuleBase {
    callPay(params: callPayParam | any): Promise<INativeResponse>;
    query(): Promise<INativeResponse>;
    topup(money: number): Promise<INativeResponse>;
    withdraw(params: IWithdrawParams | any): Promise<INativeResponse>;
    setPassword(params: passwordParam): Promise<INativeResponse>;
    bindWithdrawAccount(params: IBindParams | any): Promise<INativeResponse>;
}
export declare const Wallet: WalletModule;
export {};
