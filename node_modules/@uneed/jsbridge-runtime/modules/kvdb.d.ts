import { INativeResponse, ModuleBase } from '../native';
declare class KVDBModule extends ModuleBase {
    get(key: string, webId?: string, customTime?: number): Promise<INativeResponse>;
    remove(key: string, webId?: string): Promise<INativeResponse>;
    set(key: string, val: string, webId?: string): Promise<INativeResponse>;
    batchGet(path: Array<string> | string, webId?: string, customTime?: number): Promise<INativeResponse>;
}
export declare const KVDB: KVDBModule;
export {};
