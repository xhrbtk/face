import { INativeResponse, ModuleBase } from '../native';
interface TButton {
    action: string;
    text: string;
    style: 'normal' | 'disabled' | 'loading';
}
interface Detail {
    cvsid: string;
    label: string;
}
interface TPostEvent {
    event: string;
    data: object;
}
interface IMapMarkDest {
    cvsid: string;
    destid: string;
    mark: boolean;
}
interface CloudInfo {
    content_type: Array<string>;
    max_size: number;
}
interface fullScreenParams {
    isFullScreen: boolean;
    isHorizontal: boolean;
}
declare class ChatletModule extends ModuleBase {
    button(params: TButton): Promise<INativeResponse>;
    mapMarkUser(params: {
        uid: string;
        cvsid: string;
    } | any): Promise<INativeResponse>;
    mapMarkDest(params: IMapMarkDest): Promise<INativeResponse>;
    sharePoiSwitch(params: object): Promise<INativeResponse>;
    getCloudFile(params: CloudInfo): Promise<INativeResponse>;
    setChatletViewHeight(height: Number): Promise<INativeResponse>;
    setChatletViewFitHeight(height: Number): Promise<INativeResponse>;
    setChatletViewMinHeight(height: Number): Promise<INativeResponse>;
    setChatletViewMaxHeight(height: Number): void;
    activeChatletController(): void;
    getChatletViewThumbed(): Promise<INativeResponse>;
    setFullScreen(params: fullScreenParams): void;
    updateCvsDetailItem(params: Detail): Promise<INativeResponse>;
    getSettings(params: {
        cvsid: string;
    }): Promise<INativeResponse>;
    postEvent(params: TPostEvent): void;
    showPage(): void;
    hidePage(): void;
    getSystemVolumeOrBrightness(params: {
        type: string;
    }): Promise<INativeResponse>;
    setSystemVolumeOrBrightness(params: {
        type: string;
        percent: number;
    }): Promise<INativeResponse>;
    uploadFileToChatlet(params: {
        cvsid: string;
        multi: Boolean;
        cvsType: Number;
    }): Promise<INativeResponse>;
    startChatLiveCallback(params: {
        cvsid: string;
        isStart: Boolean;
    }): Promise<INativeResponse>;
}
export declare const Chatlet: ChatletModule;
export {};
