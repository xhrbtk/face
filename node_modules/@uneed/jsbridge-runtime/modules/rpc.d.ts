import { ModuleBase } from '../native';
declare class RPCModule extends ModuleBase {
    private EC_ID_ERROR;
    private EC_METHOD_ERROR;
    private EC_METHOD_EXEC_ERROR;
    private EC_METHOD_TIMEOUT;
    private id;
    private method;
    private requestPromise;
    constructor();
    register(id: string, method?: (params: any) => any): void;
    request(id: string, params: any): Promise<unknown>;
    response(id: string, result: any): void;
    private onRequest;
    private onResponse;
}
export declare const RPC: RPCModule;
export {};
