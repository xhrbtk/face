import { INativeResponse, ModuleBase } from '../native';
interface chatSendTextParams {
    cvsid?: string;
    cvsType?: number;
    text?: string;
    remindmap?: any;
}
interface stickerData {
    cvsid?: string;
    cvsType?: number;
    stickerObj?: object;
}
declare class ChatModule extends ModuleBase {
    sendText(params: chatSendTextParams): Promise<INativeResponse>;
    sendImage(filePath: string): Promise<INativeResponse>;
    sendSticker(params: stickerData): Promise<INativeResponse>;
    setChatAvatar(params: {
        url: object;
        type: String;
    }): Promise<INativeResponse>;
    setBackground(params: {
        cvsid: String;
        filePath: String;
        custom: boolean;
        contentType: String;
    }): Promise<INativeResponse>;
    uploadRecentMessages(params: {
        cvsid: String;
        total: number;
    }): Promise<INativeResponse>;
}
export declare const Chat: ChatModule;
export {};
