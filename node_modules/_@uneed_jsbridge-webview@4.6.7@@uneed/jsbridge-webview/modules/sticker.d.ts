import { INativeResponse, ModuleBase } from '../native';
interface addStickerParam {
    packid: string;
    url?: string;
}
interface addFavouriteParam {
    sticker_url: string;
    height?: number;
    width?: number;
}
declare class StickerModule extends ModuleBase {
    checkState(packid: string): Promise<INativeResponse>;
    download(packid: string): Promise<INativeResponse>;
    updateSticker(data: any): Promise<INativeResponse>;
    addSticker(data: addStickerParam): Promise<INativeResponse>;
    downLoadStatus(packid: string): Promise<INativeResponse>;
    addFavourite(data: addFavouriteParam): Promise<INativeResponse>;
    favoriteList(): Promise<INativeResponse>;
    refreshFavorite(): Promise<INativeResponse>;
}
export declare const Sticker: StickerModule;
export {};
