import { INativeResponse, ModuleBase } from '../native';
interface uploadParam {
    cloud?: String;
    fpid: string;
    type: number;
    ak?: String;
    bucket?: String;
}
interface saveParam {
    files: any[];
    path?: string;
    type: number;
    from?: string;
    data?: object;
}
interface fileParam {
    name?: string;
    url: string;
    size?: number;
    contentType?: string;
}
declare class NasModule extends ModuleBase {
    uploadToNas(params: uploadParam): Promise<INativeResponse>;
    saveFromNas(params: saveParam): Promise<INativeResponse>;
    fileContent(params: fileParam): Promise<INativeResponse>;
    preview(params: {
        file: any;
        from?: string;
        data?: object;
    }): Promise<INativeResponse>;
    playFile(params: {
        audioObject: object;
    }): Promise<INativeResponse>;
}
export declare const Nas: NasModule;
export {};
