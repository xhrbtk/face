import { INativeResponse, ModuleBase } from '../native';
interface TPlayOptions {
    url: string;
    loop: boolean;
    seek: number;
    channel: any;
}
interface TResume {
    channel: any;
    seek: string;
}
declare class AudioModule extends ModuleBase {
    download(url: string): Promise<INativeResponse>;
    play(params: TPlayOptions): void;
    resume(params: TResume): void;
    pause(channel: any): void;
    stop(channel: any): void;
    getState(channel: any): Promise<INativeResponse>;
}
export declare const Audio: AudioModule;
export {};
