import { INativeResponse, ModuleBase } from '../native';
declare class MemoryModule extends ModuleBase {
    get(key: string): Promise<INativeResponse>;
    set(key: string, val: string): Promise<INativeResponse>;
}
export declare const Memory: MemoryModule;
export {};
