import { INativeResponse, ModuleBase } from '../native';
interface writParams {
    path: string;
    val?: string;
    url?: string;
}
interface zipParams {
    zipFile: string;
    relative?: string;
    files: Array<string>;
}
declare class FileModule extends ModuleBase {
    read(path: string, webId?: string): Promise<INativeResponse>;
    write(data: writParams): Promise<INativeResponse>;
    remove(path: string): Promise<INativeResponse>;
    zip(data: zipParams): Promise<INativeResponse>;
    download(data: writParams): Promise<INativeResponse>;
}
export declare const File: FileModule;
export {};
